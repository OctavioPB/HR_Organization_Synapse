[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that require external services (Kafka + Postgres via docker-compose).
                 Run with: pytest -m integration

addopts =
    --tb=short
    -q
