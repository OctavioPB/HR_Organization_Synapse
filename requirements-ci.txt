# CI install — subset of requirements.txt for fast installs in GitHub Actions.
#
# Excluded intentionally:
#   torch / torch-geometric / torch-geometric-temporal
#       → torch is installed separately with the CPU-only index URL
#         (--index-url https://download.pytorch.org/whl/cpu) to avoid the 2 GB
#         CUDA download.  torch-geometric and torch-geometric-temporal are not
#         installed at all; modules that depend on them degrade gracefully
#         (_PYG_AVAILABLE = False in ml/gnn/model.py).
#   kafka-python
#       → all Kafka interaction is mocked in unit tests; no real broker is needed.
#   apache-airflow
#       → installed only inside the Docker container; see the note in requirements.txt.
#   neo4j
#       → optional graph backend; tests mock it.

# ─── Core ─────────────────────────────────────────────────────────────────────
pydantic>=2.0
python-dotenv>=1.0.0
numpy>=1.24.0
scipy>=1.11.0

# ─── Graph analytics ──────────────────────────────────────────────────────────
networkx>=3.0
python-louvain>=0.16
pandas>=2.0.0

# ─── ML ───────────────────────────────────────────────────────────────────────
scikit-learn>=1.4.0

# ─── Database (psycopg2 imported at module level; tests mock connections) ─────
psycopg2-binary>=2.9.9
sqlalchemy>=2.0

# ─── API ──────────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# ─── Supporting libraries ─────────────────────────────────────────────────────
tenacity>=8.0.0
structlog>=24.0.0
joblib>=1.3.0
redis>=5.0.0
prometheus-fastapi-instrumentator>=6.1.0
pyod>=1.1.0
anthropic>=0.40.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
hypothesis>=6.0.0
Faker>=24.0.0
respx>=0.20.0
pytest-mock>=3.14.0
